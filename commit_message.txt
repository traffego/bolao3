feat: Melhora campo afiliado_ativo com ENUM

Altera campo afiliado_ativo de TINYINT para ENUM('inativo', 'ativo'):

✅ Campo mais semântico e autodocumentado
✅ Evita valores inválidos (0/1 vs 'inativo'/'ativo')
✅ Atualiza todas as consultas SQL nos arquivos PHP
✅ Migração SQL corrigida com novo tipo de campo

Arquivos atualizados:
- migration_afiliados.sql: campo ENUM + consultas corrigidas
- login.php: consulta usando 'ativo' em vez de 1
- cadastro.php: consulta e inserção usando ENUM

Próximo: executar migração em produção