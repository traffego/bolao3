fix: Corrige script de migração para sistema unificado

Corrige migration_afiliados.sql removendo referências às tabelas afiliados antigas:

✅ Remove JOINs com tabela afiliados (que causavam erro SQL)
✅ Simplifica migração focando apenas na tabela jogador
✅ Mantém geração de codigo_afiliado para jogadores existentes
✅ Sistema totalmente unificado na tabela jogador

Arquivo corrigido:
- migration_afiliados.sql: script limpo sem dependências de tabelas antigas

Próximo: executar migração corrigida em produção